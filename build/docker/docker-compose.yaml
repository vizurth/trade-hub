volumes:
  postgres_data:


services:
  postgres:
    image: postgres:14
    env_file:
      - ../../configs/config.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data: /var/lib/postgresql/data

